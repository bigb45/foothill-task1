<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation_graph"
    app:startDestination="@id/signinFragment">
    <fragment
        android:id="@+id/signinFragment"
        android:name="com.example.logintask1.signinPackage.SigninFragment"
        android:label="SigninFragment" >
        <action
            android:id="@+id/action_signinFragment_to_signupFragment"
            app:destination="@id/signupFragment" />
        <!-- Define reverse action -->
        <action
            android:id="@+id/action_signinFragment_to_signupFragment_reversed"
            app:destination="@id/signinFragment" />
    </fragment>
    <fragment
        android:id="@+id/signupFragment"
        android:name="com.example.logintask1.signupPackage.SignupFragment"
        android:label="SignupFragment" >
        <action
            android:id="@+id/action_signupFragment_to_signinFragment"
            app:destination="@id/signinFragment" />
        <!-- Define reverse action -->
        <action
            android:id="@+id/action_signupFragment_to_signinFragment_reversed"
            app:destination="@id/signinFragment" />
    </fragment>
</navigation>
